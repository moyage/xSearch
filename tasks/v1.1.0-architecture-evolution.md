# TaskSpec: v1.1.0 Architecture Evolution (xSearch)

## 1. Metadata
- **Objective**: Refactor xSearch architecture adopting Scrapling (里世界) and Jina AI (表世界) paradigms.
- **Status**: Pending Dispatch
- **Mode**: Solo / Team-Delegation
- **Driver**: L2 Hermes + L3 (Kimi Code / OpenCode)

## 2. Requirements (DAG Breakdown)

### Phase 1: Overworld (Jina Paradigm) - `LLMDistiller`
- **Goal**: Develop `LLMDistiller.ts` to distill noisy HTML into pure Markdown.
- **Logic**: Use Mozilla Readability or a DOM cleaner + Turndown to strip noisy HTML and return strictly pure Markdown.
- **Probe/Test**: `tests/unit/LLMDistiller.test.ts` MUST exit 0 validating HTML to pure Markdown conversion.

### Phase 2: Underworld (Scrapling Paradigm) - `OpenClawBrowserEngine`
- **Goal**: Implement physical WAF Bypass (Spatial-Click) and Hybrid Escalation.
- **Logic**: 
  - Detect `challenges.cloudflare.com` iframe.
  - Implement CDP bounding-box retrieval and physical jittered mouse click (`box.x + 27`).
- **Probe/Test**: `tests/unit/OpenClawBrowserEngine.test.ts` MUST exit 0 simulating a bypass flow.

### Phase 3: Protocol API (Header/Config Hijack)
- **Goal**: Align API and `mcp-server.ts` with Header-driven controls.
- **Logic**: Parse configs like `X-Respond-With: markdown`, `X-Target-Selector` to control the `LLMDistiller` pipeline.

## 3. Review & Closure Criteria
- L4 Gatekeeper: All unit tests must pass (`exit 0`).
- Zero DOM pollution: Output format must be clean markdown by default.
- No regression on existing MCP interfaces.
